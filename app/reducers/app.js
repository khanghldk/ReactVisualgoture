const INITIAL_STATE = {
  appName: 'Algorithms Visualizer',
  url: 'http://localhost:6969/'
};

export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
